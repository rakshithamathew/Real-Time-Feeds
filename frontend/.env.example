# Optional for local development because Vite proxies to http://127.0.0.1:8001.
# Set this to the public backend origin in production; do not add a trailing slash.
VITE_BACKEND_URL=http://127.0.0.1:8001
